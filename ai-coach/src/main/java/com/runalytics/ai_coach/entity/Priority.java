package com.runalytics.ai_coach.entity;

public enum Priority {
    HIGH,    // Critical (injury risk, overload)
    MEDIUM,  // Important (optimization)
    LOW      // Suggestion (incremental improvements)
}
